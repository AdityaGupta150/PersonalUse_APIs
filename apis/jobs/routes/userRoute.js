const router = require("express").Router();

/**
 * @stalled
 * 
 * @file_brief -> The user routes are included here, ie. those that are related to the user Profile pages, logins etc
 */

/**
 * @stalled
 * @brief - login route for a user*/
router.get("/login", (req, res) => {

});

module.exports = router;
